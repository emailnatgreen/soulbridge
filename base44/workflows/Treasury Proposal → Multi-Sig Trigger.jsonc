{
  "name": "Treasury Proposal \u2192 Multi-Sig Trigger",
  "description": "When a GovernanceProposal is updated to 'passed' status, checks if it is a treasury_allocation or project_funding type and automatically initiates the multi-sig signing workflow \u2014 updating proposal status to 'executing', calculating guardian quorum, and notifying all required signers.",
  "trigger": {
    "condition": null,
    "config": {
      "trigger_type": "entity",
      "events": [
        "update"
      ],
      "entity_name": "GovernanceProposal"
    }
  },
  "definition": {
    "document": {
      "dsl": "1.0.0",
      "name": "treasury_proposal_multi_sig_trigger",
      "version": "1.0",
      "namespace": "base44"
    },
    "do": [
      {
        "run_function": {
          "call": "invoke_backend_function",
          "with": {
            "function_name": "treasuryProposalTrigger",
            "args": {}
          },
          "then": "end"
        }
      }
    ]
  },
  "x-base44-migrated-from-automation": {
    "legacy_payload_function": "treasuryProposalTrigger"
  }
}