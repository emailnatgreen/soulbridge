{
  "schema": "HonourPolicySearch_v1",
  "version": "2.0.0",
  "description": "V2 honour and sincerity rules — Phase 3 rectified with formal scoring model (diminishing returns, frequency caps, time decay).",

  "sincerity_rules": {
    "clean_aligned_query": {
      "base_delta": 0.5,
      "model": "diminishing_returns + time_decay",
      "condition": "Query is clear, non-harmful, and purpose-aligned per 7-Leaf evaluation",
      "alignment_score_min": 0.6,
      "collective_risk_score_max": 0.3,
      "frequency_cap": { "max_per_day": 50, "max_per_hour": 10 }
    },
    "neutral_query": {
      "base_delta": 0,
      "condition": "Query is ambiguous or low-signal but not harmful",
      "alignment_score_range": [0.3, 0.6],
      "collective_risk_score_max": 0.6
    },
    "spam_pattern": {
      "base_delta": -1.0,
      "model": "flat (penalty — no diminishing returns on violations)",
      "condition": "Spammy, repetitive, or low-effort manipulative pattern detected",
      "triggers": ["repeated_identical_queries", "high_frequency_burst", "keyword_stuffing"]
    },
    "exploit_pattern": {
      "base_delta": -2.0,
      "model": "flat (penalty — no diminishing returns on violations)",
      "condition": "Exploitative, manipulative, or socially damaging intent detected",
      "triggers": ["social_engineering", "scam_research", "harm_intent", "deception_amplification"]
    }
  },

  "collective_risk_thresholds": {
    "clean": { "max": 0.3, "action": "allow" },
    "caution": { "min": 0.3, "max": 0.6, "action": "allow_with_note" },
    "warn": { "min": 0.6, "max": 0.85, "action": "warn_user" },
    "block": { "min": 0.85, "action": "block_search" }
  },

  "gating_rules": {
    "min_sincerity_to_search": 20,
    "sincerity_recovery_path": "Blocked DIDs must wait 24h or complete a guided repair to restore search access.",
    "cooldown_on_block_seconds": 300
  },

  "honour_integration": {
    "sincerity_feeds_into": "agent.honour_score (via diminishing returns + time decay)",
    "log_entity": "SearchLog",
    "audit_trail": true,
    "formal_model": "base × ln(2)/ln(1+count) × 0.5^(age/30d)"
  },

  "fair_share_compliance": {
    "treasury_share_percent": 2,
    "extraction_ceiling": 2.5,
    "phase3_rectified": true,
    "previous_value": 20,
    "law3_alignment": "Platform extraction reduced from 20% to 2% — within 2.5% ceiling"
  }
}